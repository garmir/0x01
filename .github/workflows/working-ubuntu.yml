name: Working Ubuntu Test - 0x01
on: [push, workflow_dispatch]

jobs:
  ubuntu-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Ubuntu Tools Installation
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq coreutils

      - name: Working Ubuntu Test - 0x01
        run: |
          echo "=== Working Ubuntu Test: 0x01 ===" > ubuntu-test-0x01.txt
          echo "Repository: ${{ github.repository }}" >> ubuntu-test-0x01.txt
          echo "Test ID: UBUNTU_001" >> ubuntu-test-0x01.txt
          echo "Timestamp: $(date)" >> ubuntu-test-0x01.txt
          curl -s ifconfig.me >> ubuntu-test-0x01.txt
          echo "Instruction: Ubuntu methodology validation for 0x01" >> ubuntu-test-0x01.txt
          echo "✅ Ubuntu approach successful" >> ubuntu-test-0x01.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: ubuntu-test-0x01
          path: "*.txt"
          retention-days: 2